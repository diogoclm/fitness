# Chave da API da Anthropic (Claude). Fica SÓ no servidor — não vai pro navegador.
# Local: usada pelo proxy de dev do Vite (npm run dev).
# Produção (Vercel): defina esta variável em Settings > Environment Variables.
ANTHROPIC_API_KEY=
